# PRD: randpw - CLI 패스워드 생성기

## MVP 캡슐

1. **목표(Outcome)**: 안전한 랜덤 패스워드를 CLI에서 즉시 생성
2. **페르소나/타깃 사용자**: 개발자, 시스템 관리자, CLI 파워 유저
3. **핵심 가치 제안**: `npx randpw` 한 줄로 암호학적으로 안전한 패스워드 즉시 생성
4. **EPIC-1**: 패스워드 생성 기능
5. **FEAT-1**: 옵션별 패스워드 생성 (길이, 문자종류, 개수 지정)
6. **노스스타 지표**: npm 주간 다운로드 수
7. **입력 지표**: (1) GitHub 스타 수, (2) CLI 실행 성공률 (에러율 < 0.1%)
8. **Non-goals**: (1) GUI/웹 인터페이스, (2) 패스워드 저장/관리, (3) 클라우드 동기화
9. **NFR Top 2**: (1) crypto.getRandomValues 기반 보안 랜덤 (NFR-1), (2) 응답 시간 < 50ms (NFR-2)
10. **데이터 민감도**: PII 없음, 상태 저장 없음, 생성된 패스워드는 stdout으로만 출력 후 보관하지 않음
11. **Top 리스크**: Math.random() 사용 시 예측 가능한 패스워드 생성 → 완화: node:crypto 모듈 강제 사용, lint 규칙으로 Math.random 금지
12. **다음 7일 액션**: MVP 구현 → 테스트 → npm publish

---

## 1. Problem Statement

개발자와 시스템 관리자는 서버 설정, 계정 생성, 환경변수 구성 등에서 안전한 패스워드가 필요하다. 기존 방법은 다음과 같은 문제를 갖는다:

- **온라인 생성기**: 네트워크 전송 중 패스워드 노출 위험
- **수동 생성**: 예측 가능한 패턴, 충분하지 않은 엔트로피
- **openssl rand**: 문자셋 제어 불가, 특수문자 포함 여부 불확실

`randpw`는 로컬에서 암호학적으로 안전한 랜덤 패스워드를 즉시 생성하여 이 문제를 해결한다.

> **근거**: D-01 (단순하고 실용적, 외부 API 불필요), D-07 (stateless 아키텍처)

## 2. Goals

- 한 줄 명령어(`npx randpw`)로 안전한 패스워드 즉시 생성
- 길이, 문자셋, 개수를 유연하게 커스터마이징
- 크로스플랫폼 지원 (Linux/macOS/Windows)
- npm 패키지로 배포하여 설치 없이 즉시 사용 가능

## 3. Non-goals (이번 릴리스 제외)

- GUI/웹 인터페이스 구축
- 패스워드 저장/관리 기능 (패스워드 매니저 영역)
- 클라우드 동기화 또는 원격 저장
- 클립보드 자동 복사 (v2에서 고려, D-08)
- 패스워드 강도 표시 (v2에서 고려, D-06)
- Homebrew/바이너리 배포 (v2에서 고려, D-09)

## 4. Personas & Use Cases

### 4.1 페르소나

| 페르소나 | 설명 | 주요 니즈 |
|----------|------|-----------|
| **개발자** | 서버 설정, .env 파일, API 키 생성 시 안전한 값 필요 | 빠른 생성, 파이프라인 연동 |
| **시스템 관리자** | 다수의 서버/계정 패스워드 일괄 생성 | 다중 생성, 문자셋 제어 |
| **CLI 파워 유저** | 셸 스크립트 내에서 자동화 | stdout 출력, 종료 코드 |

### 4.2 Use Cases

| ID | 사용자 | 시나리오 | 기대 결과 |
|----|--------|----------|-----------|
| UC-1 | 개발자 | 터미널에서 `npx randpw` 실행 | 16자 랜덤 패스워드 1개 출력 |
| UC-2 | 시스템 관리자 | `randpw -l 32 -c 10` 실행 | 32자 패스워드 10개 출력 |
| UC-3 | 개발자 | `randpw --no-symbols -l 24` 실행 | 특수문자 없는 24자 패스워드 출력 |
| UC-4 | CLI 파워 유저 | `randpw \| pbcopy` 파이프 사용 | 패스워드가 클립보드로 전달 |
| UC-5 | 사용자 | 모든 문자셋을 제외하고 실행 | 명확한 에러 메시지와 종료코드 1 |

## 5. 요구사항 (EPIC / FEAT / REQ)

### EPIC-1: 패스워드 생성 기능

패스워드 생성의 전체 사용자 가치를 제공하는 핵심 에픽.

#### FEAT-1: 기본 패스워드 생성

기본 옵션(16자, 모든 문자셋 포함)으로 패스워드를 생성한다.

- **REQ-1**: `node:crypto.getRandomValues()` 기반으로 암호학적으로 안전한 랜덤 값을 생성해야 한다.
- **REQ-4**: 생성된 패스워드는 한 줄에 하나씩 stdout으로 출력해야 한다.

#### FEAT-2: 길이 커스터마이징

`-l, --length <number>` 옵션으로 패스워드 길이를 지정한다.

- **REQ-3**: 길이는 1~1024 범위로 검증하며, 범위 밖의 값은 에러를 반환해야 한다.

#### FEAT-3: 문자셋 필터링

`--no-uppercase`, `--no-lowercase`, `--no-numbers`, `--no-symbols` 옵션으로 문자셋을 제어한다.

- **REQ-2**: 최소 1개 문자셋은 반드시 포함되어야 하며, 모든 문자셋 제외 시 에러를 반환해야 한다.

#### FEAT-4: 다중 생성

`-c, --count <number>` 옵션으로 여러 패스워드를 한 번에 생성한다.

- **REQ-4** (참조): 각 패스워드는 줄바꿈으로 구분하여 stdout으로 출력해야 한다.
- **REQ-5**: count는 1 이상의 양의 정수여야 하며, 유효하지 않은 값은 에러를 반환해야 한다.

#### FEAT-5: 도움말 및 버전 표시

`-h, --help`, `-V, --version` 옵션으로 사용법과 버전 정보를 표시한다.

## 6. Success Metrics

### 노스스타 지표
- **npm 주간 다운로드 수**: 출시 후 1개월 내 100+ 달성 목표

### Leading 지표
| 지표 | 목표 | 측정 방법 |
|------|------|-----------|
| GitHub 스타 수 | 출시 1개월 내 10+ | GitHub API |
| CLI 실행 성공률 | 에러율 < 0.1% | 에러 리포트 기반 |

## 7. Assumptions

- 사용자는 Node.js 18+ 환경이 설치되어 있다 (NFR-3)
- 사용자는 터미널/CLI 사용에 익숙하다
- `node:crypto` 모듈이 모든 지원 플랫폼에서 동작한다
- npm 레지스트리를 통한 배포가 가장 접근성이 높다 (D-09)

## 8. Risks & Experiment Plan

| ID | 리스크 | 영향 | 확률 | 완화 전략 |
|----|--------|------|------|-----------|
| RISK-1 | Math.random() 사용 시 예측 가능한 패스워드 생성 | 치명적 | 낮음 | `node:crypto` 모듈 강제 사용, ESLint 규칙으로 Math.random 사용 금지 |
| RISK-2 | 모든 문자셋 제외 시 생성 불가 | 중간 | 중간 | 입력 검증으로 차단, 명확한 에러 메시지 제공 |

### 실험 계획
- RISK-1 검증: 생성된 패스워드의 엔트로피 분포 테스트 (chi-squared test)
- RISK-2 검증: 모든 옵션 조합에 대한 경계값 테스트

## 9. Release Scope & Milestones

| 마일스톤 | 내용 | 기간 |
|----------|------|------|
| M0 | 프로젝트 환경 설정 (레포, 빌드, 테스트, 린트) | 1일 |
| M1 | 핵심 생성 로직 구현 (FEAT-1, FEAT-2) | 1~2일 |
| M2 | CLI 통합 및 옵션 구현 (FEAT-3, FEAT-4, FEAT-5) | 1~2일 |
| M3 | 테스트, 문서화, npm publish (D-09) | 1~2일 |

> **참고**: 추가 레퍼런스 문서가 제공되지 않았으므로, 일반 모범사례 기반으로 작성하였습니다.
